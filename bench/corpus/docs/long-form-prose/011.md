# Notes on garbage collection (doc 11)

## Background

Paragraph 1 discusses garbage collection in document 11. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

## Discussion

Paragraph 2 discusses garbage collection in document 11. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 3 discusses garbage collection in document 11. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 4 discusses garbage collection in document 11. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 5 discusses garbage collection in document 11. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 6 discusses garbage collection in document 11. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

> A blockquote observation about garbage collection, doc 11.
