# Notes on CRDT merge semantics (doc 44)

## Background

Paragraph 1 discusses CRDT merge semantics in document 44. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

## Discussion

Paragraph 2 discusses CRDT merge semantics in document 44. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 3 discusses CRDT merge semantics in document 44. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 4 discusses CRDT merge semantics in document 44. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 5 discusses CRDT merge semantics in document 44. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

Paragraph 6 discusses CRDT merge semantics in document 44. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. This is ordinary prose with *emphasis*, **strong emphasis**, and the occasional `inline code` reference. 

> A blockquote observation about CRDT merge semantics, doc 44.
