# Code sample set 3

Intro text before the first fence, doc 3.

```go
// snippet 0 in doc 3
function f0(x) {
  return x + 0;
}
```

```go
// snippet 1 in doc 3
function f1(x) {
  return x + 1;
}
```

```go
// snippet 2 in doc 3
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
