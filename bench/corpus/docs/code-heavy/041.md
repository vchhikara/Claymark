# Code sample set 41

Intro text before the first fence, doc 41.

```python
// snippet 0 in doc 41
function f0(x) {
  return x + 0;
}
```

```python
// snippet 1 in doc 41
function f1(x) {
  return x + 1;
}
```

```python
// snippet 2 in doc 41
function f2(x) {
  return x + 2;
}
```

```python
// snippet 3 in doc 41
function f3(x) {
  return x + 3;
}
```

```python
// snippet 4 in doc 41
function f4(x) {
  return x + 4;
}
```

Some trailing prose with an `inline` reference after the fences.
