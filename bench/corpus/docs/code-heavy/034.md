# Code sample set 34

Intro text before the first fence, doc 34.

```bash
// snippet 0 in doc 34
function f0(x) {
  return x + 0;
}
```

```bash
// snippet 1 in doc 34
function f1(x) {
  return x + 1;
}
```

```bash
// snippet 2 in doc 34
function f2(x) {
  return x + 2;
}
```

```bash
// snippet 3 in doc 34
function f3(x) {
  return x + 3;
}
```

Some trailing prose with an `inline` reference after the fences.
