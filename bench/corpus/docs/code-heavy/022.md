# Code sample set 22

Intro text before the first fence, doc 22.

```rust
// snippet 0 in doc 22
function f0(x) {
  return x + 0;
}
```

```rust
// snippet 1 in doc 22
function f1(x) {
  return x + 1;
}
```

```rust
// snippet 2 in doc 22
function f2(x) {
  return x + 2;
}
```

```rust
// snippet 3 in doc 22
function f3(x) {
  return x + 3;
}
```

Some trailing prose with an `inline` reference after the fences.
