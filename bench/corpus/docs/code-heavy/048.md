# Code sample set 48

Intro text before the first fence, doc 48.

```go
// snippet 0 in doc 48
function f0(x) {
  return x + 0;
}
```

```go
// snippet 1 in doc 48
function f1(x) {
  return x + 1;
}
```

```go
// snippet 2 in doc 48
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
