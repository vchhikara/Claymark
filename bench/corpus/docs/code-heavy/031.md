# Code sample set 31

Intro text before the first fence, doc 31.

```python
// snippet 0 in doc 31
function f0(x) {
  return x + 0;
}
```

```python
// snippet 1 in doc 31
function f1(x) {
  return x + 1;
}
```

```python
// snippet 2 in doc 31
function f2(x) {
  return x + 2;
}
```

```python
// snippet 3 in doc 31
function f3(x) {
  return x + 3;
}
```

Some trailing prose with an `inline` reference after the fences.
