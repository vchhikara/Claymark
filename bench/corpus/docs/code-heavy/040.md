# Code sample set 40

Intro text before the first fence, doc 40.

```ts
// snippet 0 in doc 40
function f0(x) {
  return x + 0;
}
```

```ts
// snippet 1 in doc 40
function f1(x) {
  return x + 1;
}
```

```ts
// snippet 2 in doc 40
function f2(x) {
  return x + 2;
}
```

```ts
// snippet 3 in doc 40
function f3(x) {
  return x + 3;
}
```

Some trailing prose with an `inline` reference after the fences.
