# Code sample set 8

Intro text before the first fence, doc 8.

```go
// snippet 0 in doc 8
function f0(x) {
  return x + 0;
}
```

```go
// snippet 1 in doc 8
function f1(x) {
  return x + 1;
}
```

```go
// snippet 2 in doc 8
function f2(x) {
  return x + 2;
}
```

```go
// snippet 3 in doc 8
function f3(x) {
  return x + 3;
}
```

```go
// snippet 4 in doc 8
function f4(x) {
  return x + 4;
}
```

Some trailing prose with an `inline` reference after the fences.
