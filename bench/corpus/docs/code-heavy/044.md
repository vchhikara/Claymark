# Code sample set 44

Intro text before the first fence, doc 44.

```bash
// snippet 0 in doc 44
function f0(x) {
  return x + 0;
}
```

```bash
// snippet 1 in doc 44
function f1(x) {
  return x + 1;
}
```

```bash
// snippet 2 in doc 44
function f2(x) {
  return x + 2;
}
```

```bash
// snippet 3 in doc 44
function f3(x) {
  return x + 3;
}
```

```bash
// snippet 4 in doc 44
function f4(x) {
  return x + 4;
}
```

Some trailing prose with an `inline` reference after the fences.
