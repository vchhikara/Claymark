# Code sample set 12

Intro text before the first fence, doc 12.

```rust
// snippet 0 in doc 12
function f0(x) {
  return x + 0;
}
```

```rust
// snippet 1 in doc 12
function f1(x) {
  return x + 1;
}
```

```rust
// snippet 2 in doc 12
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
