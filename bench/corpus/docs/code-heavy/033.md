# Code sample set 33

Intro text before the first fence, doc 33.

```go
// snippet 0 in doc 33
function f0(x) {
  return x + 0;
}
```

```go
// snippet 1 in doc 33
function f1(x) {
  return x + 1;
}
```

```go
// snippet 2 in doc 33
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
