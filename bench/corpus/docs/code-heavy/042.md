# Code sample set 42

Intro text before the first fence, doc 42.

```rust
// snippet 0 in doc 42
function f0(x) {
  return x + 0;
}
```

```rust
// snippet 1 in doc 42
function f1(x) {
  return x + 1;
}
```

```rust
// snippet 2 in doc 42
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
