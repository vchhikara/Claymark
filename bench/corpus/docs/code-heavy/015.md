# Code sample set 15

Intro text before the first fence, doc 15.

```ts
// snippet 0 in doc 15
function f0(x) {
  return x + 0;
}
```

```ts
// snippet 1 in doc 15
function f1(x) {
  return x + 1;
}
```

```ts
// snippet 2 in doc 15
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
