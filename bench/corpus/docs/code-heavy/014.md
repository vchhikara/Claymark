# Code sample set 14

Intro text before the first fence, doc 14.

```bash
// snippet 0 in doc 14
function f0(x) {
  return x + 0;
}
```

```bash
// snippet 1 in doc 14
function f1(x) {
  return x + 1;
}
```

```bash
// snippet 2 in doc 14
function f2(x) {
  return x + 2;
}
```

```bash
// snippet 3 in doc 14
function f3(x) {
  return x + 3;
}
```

```bash
// snippet 4 in doc 14
function f4(x) {
  return x + 4;
}
```

Some trailing prose with an `inline` reference after the fences.
