# Code sample set 43

Intro text before the first fence, doc 43.

```go
// snippet 0 in doc 43
function f0(x) {
  return x + 0;
}
```

```go
// snippet 1 in doc 43
function f1(x) {
  return x + 1;
}
```

```go
// snippet 2 in doc 43
function f2(x) {
  return x + 2;
}
```

```go
// snippet 3 in doc 43
function f3(x) {
  return x + 3;
}
```

Some trailing prose with an `inline` reference after the fences.
