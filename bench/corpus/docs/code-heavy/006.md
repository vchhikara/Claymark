# Code sample set 6

Intro text before the first fence, doc 6.

```python
// snippet 0 in doc 6
function f0(x) {
  return x + 0;
}
```

```python
// snippet 1 in doc 6
function f1(x) {
  return x + 1;
}
```

```python
// snippet 2 in doc 6
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
