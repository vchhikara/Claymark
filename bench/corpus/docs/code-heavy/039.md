# Code sample set 39

Intro text before the first fence, doc 39.

```bash
// snippet 0 in doc 39
function f0(x) {
  return x + 0;
}
```

```bash
// snippet 1 in doc 39
function f1(x) {
  return x + 1;
}
```

```bash
// snippet 2 in doc 39
function f2(x) {
  return x + 2;
}
```

Some trailing prose with an `inline` reference after the fences.
